# Supabase PostgreSQL connection string used by Prisma.
# Use the connection pooler URL for deployed/serverless environments.
DATABASE_URL="postgresql://postgres:[PASSWORD]@[PROJECT-REF].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Used by the JWT session cookie.
JWT_SECRET="replace-with-a-long-random-secret"